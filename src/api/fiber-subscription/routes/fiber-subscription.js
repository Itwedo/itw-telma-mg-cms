'use strict';

/**
 * fiber-subscription router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::fiber-subscription.fiber-subscription');
